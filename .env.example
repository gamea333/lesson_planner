# Groq API key — get one at https://console.groq.com
# Paste your key after the equals sign (keep this in .env.local — never commit):
GROQ_API_KEY=

# Optional: seed a WhatsApp number. Prefer setting it in the app via
# Navbar → WhatsApp (saved in your browser only).
# NEXT_PUBLIC_WHATSAPP_NUMBER=
